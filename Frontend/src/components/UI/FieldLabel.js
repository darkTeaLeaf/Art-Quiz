import styled from "styled-components";

const FieldLabel = styled.span`
  position: absolute;
  right: calc(100% + 15px);
  height: 100%;
  font-family: Raleway;
  font-size: 20px;
  font-weight: 400;
  line-height: 20px;
  color: lightgrey;
  white-space: nowrap;
  display: flex;
  align-items: center;
`;

export default FieldLabel;
